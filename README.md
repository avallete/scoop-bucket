# scoop-bucket

Scoop bucket used to validate the TypeScript CLI release pipeline
(see dx-lab ADR 0011). Non-production, do not install in production.

Install:
```
scoop bucket add avallete-poc https://github.com/avallete/scoop-bucket
scoop install supabase-shim-poc
```

